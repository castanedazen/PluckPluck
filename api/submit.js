module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'POST required'});const{caseId,route,complaint}=req.body||{};if(!caseId||!route||!complaint)return res.status(400).json({error:'Missing case, route, or complaint'});const requiresHuman=['web_login'].includes(route.mode);const next=requiresHuman?'authentication_required':'external_form_required';return res.status(200).json({caseId,state:next,agency:route.agency,verifiedUrl:route.url,email:route.email||null,message:requiresHuman?'Agency authentication is required. Continue to the verified official site; after sign-in, use the prepared complaint.':'No direct submission adapter is installed for this verified route yet. Continue to the verified official site with the complaint already prepared.',timestamp:new Date().toISOString()})};