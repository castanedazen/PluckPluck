 KNOW',have],['missing','WHAT IS MISSING',missing.length?missing:['No major factual gap detected from the information provided.']],['boost','WHAT COULD STRENGTHEN THIS',boost],['noise','WHAT PROBABLY DOESN\'T MATTER',noise]].map(([cls,t,arr])=>`<div class="bucket ${cls}"><h3>${t}</h3><ul>${arr.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`).join('');show('strength')}
function memoText(){return `SUBJECT: Request for review concerning ${state.name||'reported organization'}\n\nFACTUAL SUMMARY\nThe complainant reports the following:\n\n${state.story}\n\nWHEN IT HAPPENED\n${state.incidentDate||state.incidentApprox||'Not provided'}\n\nLOCATION\n${state.where||'Not provided'}\n\nRESPONSIBLE PARTY / TYPE\n${state.name||'Name not provided'}\n${Array.isArray(state.who)?state.who.join(', '):state.who}\n\nREPORTED IMPACT\n${list(state.impact,['Not specified']).map(x=>'• '+x).join('\n')}\n\nPRIOR RESOLUTION ATTEMPTS\n${list(state.tried,['Not specified']).map(x=>'• '+x).join('\n')}\n\nAVAILABLE EVIDENCE\n${list(state.evidence,['Not specified']).map(x=>'• '+x).join('\n')}\n\nREQUESTED ACTION\n${list(state.outcomes,['Review and appropriate action within the agency’s authority']).map(x=>'• '+x).join('\n')}\n\nThis complaint asks the receiving authority to review the reported facts and determine whether any law, regulation, licensing standard, contractual obligation, or other requirement within its jurisdiction may have been violated. No allegation is presented here as an adjudicated fact.`}
function saveIndex(c){const a=caseIndex();const i=a.findIndex(x=>x.case_number===c.case_number);if(i>=0)a[i]={...a[i],...c};else a.unshift(c);localStorage.setItem('dsa_case_index',JSON.stringify(a.slice(0,30)))}
function caseIndex(){try{return JSON.parse(localStorage.getItem('dsa_case_index')||'[]')}catch{return[]}}
async function buildPlan(){renderRail('planRail',5);$('routes').innerHTML='<div class="panel"><h2>BUILDING THE ROUTING PLAN…</h2><p>Checking the verified adapter registry. Nothing is being filed.</p></div>';$('shortSummary').textContent=state.story;$('evidenceSummary').innerHTML=state.evidence.map(x=>`<div>✓ ${esc(x)}</div>`).join('');$('memo').textContent=memoText();show('plan');try{const j=await call({action:'create_case',original_story:state.story,location_text:state.where,incident_date:state.incidentDate||null,incident_date_text:state.incidentApprox||state.incidentDate||null,subject_name:state.name,subject_types:Array.isArray(state.who)?state.who:[state.who],problem_types:state.impact,impacts:state.impact,prior_actions:state.tried,evidence_types:state.evidence,desired_outcomes:state.outcomes});routes=j.routes||[];caseNum=j.case.case_number;token=j.guest_token;$('caseNo').textContent=`CASE ${caseNum}`;saveIndex({case_number:caseNum,guest_token:token,subject:state.name,where:state.where,incidentDate:state.incidentDate,incidentApprox:state.incidentApprox,story:state.story,status:j.case.status||'prepared'});try{await uploadCaseFiles()}catch(uploadErr){toast('Case created. One attachment could not be stored: '+uploadErr.message)}renderRoutes()}catch(e){$('routes').innerHTML=`<div class="panel"><h2>WE HIT A ROUTING PROBLEM.</h2><p>${esc(e.message)}</p><p><b>Nothing was filed.</b></p></div>`;$('primaryAction').disabled=true}}
function friendlyMethod(r){return r.submission_method==='web_form'?'ONLINE COMPLAINT FORM':r.submission_method==='phone'?'PHONE / ADVOCACY ROUTE':(r.submission_method||'OFFICIAL PROCESS').replaceAll('_',' ').toUpperCase()}
function routeWhy(r){if(r.reason)return r.reason;if(r.plain_english)return r.plain_english;return'This authority appears relevant based on the organization type, reported harm, and location.'}
function renderRoutes(){if(!routes.length){$('planHeadline').textContent='WE NEED A BETTER ROUTE.';$('routes').innerHTML='<div class="panel"><p>No verified route was strong enough to recommend. Nothing was filed.</p></div>';$('primaryAction').disabled=true;return}$('planHeadline').textContent=`WE FOUND ${routes.length} ${routes.length===1?'WAY':'WAYS'} TO GO AFTER THIS.`;$('routes').innerHTML=routes.map((r,i)=>`<article class="route ${i===0?'primary':'additional'}"><div><span class="tag ${i===0?'acid':''}">${esc((r.role|| (i===0?'primary':'additional')).toUpperCase())}</span><h3>${esc(r.agency_name)}</h3><div class="why"><b>WHY THIS MAY APPLY</b><br>${esc(routeWhy(r))}</div><div class="routeMeta"><div><b>WHO THEY ARE</b>${esc(r.plain_english||'A public authority with a complaint process in this category.')}</div><div><b>WHAT THEY CAN ACTUALLY DO</b>${esc(r.authority_scope||'Review complaints within its authority and decide whether further action is warranted.')}</div><div><b>FILING METHOD</b>${esc(friendlyMethod(r))}</div><div><b>DSA STATUS</b>${r.supports_direct_automation?'Verified automation path':'Human submission required by this agency'}</div><div><b>WHAT DSA CAN DO</b>${r.supports_direct_automation?'Prepare, authorize, transmit through the verified adapter, and wait for external proof.':'Prepare the factual complaint, organize the evidence plan, open the exact official process, and track the receipt afterward.'}</div><div><b>WHAT YOU MUST DO</b>${r.supports_direct_automation?'Review and authorize before anything is sent.':esc((r.requirements&&r.requirements.human_step)||'Complete the agency-controlled submission step and bring the confirmation back to DSA.')}</div></div>${r.expected_response_days?`<p class="micro">Response marker in registry: about ${esc(r.expected_response_days)} days. Actual timing can vary.</p>`:''}${r.last_verified_at?`<p class="micro">ROUTE REGISTRY VERIFIED: ${new Date(r.last_verified_at).toLocaleDateString()}</p>`:''}</div><div><span class="status ${r.supports_direct_automation?'auto':'human'}">${r.supports_direct_automation?'DSA CAN CARRY THIS':'HUMAN SUBMISSION REQUIRED'}</span></div></article>`).join('');selectedRoute=routes[0];$('primaryAction').textContent=selectedRoute.supports_direct_automation?'REVIEW WHAT WILL BE SENT →':'SHOW ME EXACTLY WHAT I HAVE TO DO →';$('primaryAction').disabled=false}
function copyMemo(){navigator.clipboard.writeText(memoText()).then(()=>toast('Complaint copied.')).catch(()=>toast('Select and copy the complaint manually.'))}
async function handoff(){if(!selectedRoute)return;const r=selectedRoute;try{await call({action:'authorize',case_number:caseNum,guest_token:token,route_id:r.id})}catch(e){toast(e.message);return}saveIndex({case_number:caseNum,guest_token:token,subject:state.name,where:state.where,story:state.story,status:r.supports_direct_automation?'authorized':'needs_user_action'});$('handoffTitle').textContent=r.supports_direct_automation?'READY FOR YOUR AUTHORIZATION.':'WE DID THE PREP. THIS IS THE LAST HUMAN STEP.';$('handoffText').textContent=r.supports_direct_automation?`DSA has a verified automation path for ${r.agency_name}. We still do not call it filed until external confirmation exists.`:`${r.agency_name} requires a human interaction in its official process. We reduced your job to the smallest legitimate handoff we can verify.`;$('prepared').innerHTML=['Complaint narrative','Material facts and requested action','Evidence checklist',`Primary route: ${r.agency_name}`].map(x=>`<div class="why">✓ ${esc(x)}</div>`).join('');$('handoffSteps').innerHTML=r.supports_direct_automation?`<div class="step"><h3>Review the submission</h3><p>Confirm the facts, route, and evidence.</p></div><div class="step"><h3>Authorize DSA</h3><p>Authorization is recorded before any external submission attempt.</p></div><div class="step"><h3>Wait for proof</h3><p>DSA does not mark the case filed until there is a real external confirmation.</p></div>`:`<div class="step"><h3>Open the exact official process</h3><p>Use the complaint page DSA identified — not a generic agency homepage.</p></div><div class="step"><h3>Paste the complaint and attach relevant evidence</h3><p>Use the prepared narrative and only the evidence that supports what you reported.</p></div><div class="step"><h3>Complete the agency-only step</h3><p>Finish any identity, attestation, login, CAPTCHA, or signature requirement the agency itself controls.</p></div><div class="step"><h3>Bring back the confirmation number</h3><p>DSA uses that receipt to move the route from “human step required” to “confirmation received.”</p></div><div class="routeActionNote"><b>Estimated hands-on time: about 4 minutes</b><br>DSA has already done the complaint drafting, route selection, and evidence preparation. The remaining step belongs to the agency.</div>`;$('handoffActions').innerHTML=`${r.official_url?`<a class="btn red" href="${esc(r.official_url)}" target="_blank" rel="noopener">OPEN OFFICIAL COMPLAINT PROCESS →</a>`:''}<button class="btn ghost" id="copy2">COPY MY COMPLAINT</button><button class="btn black" id="submitted">I SUBMITTED IT →</button>`;show('handoff');$('copy2').onclick=copyMemo;$('submitted').onclick=()=>{$('confirmPanel').classList.remove('hidden');$('confirmation').focus()}}
async function saveConfirmation(){const v=$('confirmation').value.trim();if(!v){toast('Enter the agency confirmation number.');return}try{const j=await call({action:'confirm_filed',case_number:caseNum,guest_token:token,route_id:selectedRoute?.id,confirmation_number:v});saveIndex({case_number:caseNum,guest_token:token,subject:state.name,where:state.where,story:state.story,status:j.status,confirmation:v});toast('Confirmation saved. Now DSA can track the case truthfully.');openCase(caseNum)}catch(e){toast(e.message)}}
function renderCases(){const a=caseIndex();$('caseList').innerHTML=a.length?a.map(c=>`<button class="route caseOpen" type="button" data-id="${esc(c.case_number)}" style="width:100%;text-align:left"><div><span class="tag">${esc((c.status||'saved').replaceAll('_',' ').toUpperCase())}</span><h3>${esc(c.subject||'CASE')}</h3><div class="micro">${esc(c.case_number)} · ${esc(c.where||'')}</div><p>${esc((c.story||'').slice(0,180))}${(c.story||'').length>180?'…':''}</p></div><span class="status">CONTINUE CASE →</span></button>`).join(''):'<div class="empty"><b>NO CASES YET.</b><br>Start by telling DSA what happened.</div>';$$('.caseOpen').forEach(b=>b.onclick=()=>openCase(b.dataset.id))}
async function openCase(id){const idx=caseIndex().find(x=>x.case_number===id);if(!idx)return;activeCase=idx;caseNum=idx.case_number;token=idx.guest_token;show('caseDetail');$('detailTitle').textContent=idx.subject||'CASE';$('detailNo').textContent=idx.case_number;$('detailStatus').textContent=(idx.status||'loading').replaceAll('_',' ').toUpperCase();$('detailBody').innerHTML='<p>Loading secure case record…</p>';$('timeline').innerHTML='';try{const j=await call({action:'get_case',case_number:idx.case_number,guest_token:idx.guest_token});activeCase={...idx,server:j};const c=j.case||{};$('detailStatus').textContent=(c.status||idx.status||'saved').replaceAll('_',' ').toUpperCase();$('detailTitle').textContent=c.subject_name||idx.subject||'CASE';$('detailNo').textContent=c.case_number||idx.case_number;const firstRoute=(j.routes||[])[0]||null;const actionButtons=[];if(c.status==='needs_user_action'&&firstRoute?.official_url)actionButtons.push(`<a class="btn red" target="_blank" rel="noopener" href="${esc(firstRoute.official_url)}">OPEN OFFICIAL FILING →</a>`);actionButtons.push(`<button class="btn ghost" type="button" id="copyCaseComplaint">COPY COMPLAINT</button>`);$('nextAction').innerHTML=`<div class="statusTruth"><div class="live"><b>WHAT'S HAPPENING?</b>${esc((c.status||idx.status||'saved').replaceAll('_',' '))}</div><div><b>WHAT HAPPENS NEXT?</b>${esc(c.next_action||nextActionText(c.status))}</div><div><b>DO I NEED TO DO ANYTHING?</b>${c.status==='needs_user_action'?'Yes — complete the official agency step below.':'Nothing right now unless DSA shows an action.'}</div></div>${c.next_action_due_at?`<p class="micro">NEXT CHECK: ${new Date(c.next_action_due_at).toLocaleString()}</p>`:''}<div class="row">${actionButtons.join('')}</div>`;$('detailBody').innerHTML=`<p><b>When:</b> ${esc(c.incident_date_text||c.incident_date||idx.incidentDate||idx.incidentApprox||'Not provided')}</p><p><b>Location:</b> ${esc(c.location_text||idx.where||'')}</p><p><b>Category:</b> ${esc(c.category||'')}</p><p><b>Problem summary:</b><br>${esc(c.original_story||idx.story||'')}</p>`;const ev=j.events||[];$('timeline').innerHTML=ev.length?ev.map(e=>`<div class="event"><b>${esc(e.title||e.event_type)}</b><br><small>${new Date(e.created_at).toLocaleString()}</small><p>${esc(e.detail||'')}</p></div>`).join(''):'<div class="event"><b>Case created</b></div>';const evidence=j.evidence||[];$('detailEvidence').innerHTML=evidence.length?evidence.map(e=>`<div class="why"><b>${esc(e.file_name)}</b><br><span class="micro">${esc(e.evidence_type||'evidence')} · ${esc(e.status||'saved')}</span></div>`).join(''):'<div class="empty">No evidence metadata saved yet.</div>';const responses=j.responses||[];$('responseList').innerHTML=responses.length?responses.map(r=>`<details><summary>${esc(r.source_label||'Agency response')} · ${new Date(r.received_at).toLocaleDateString()}</summary><div class="inside"><p>${esc(r.body||'')}</p></div></details>`).join(''):'';selectedRoute=firstRoute;const cc=$('copyCaseComplaint');if(cc)cc.onclick=()=>{const text=c.generated_summary?.what_happened||c.original_story||idx.story||'';navigator.clipboard.writeText(text).then(()=>toast('Complaint text copied.')).catch(()=>toast('Copy unavailable.'))}}catch(e){$('detailBody').innerHTML=`<p>Could not load the secure case record: ${esc(e.message)}</p>`}}
function nextActionText(s){if(s==='confirmation_received')return'DSA has confirmation. Watch for the agency response and add it here when it arrives.';if(s==='response_received')return'Review the agency response and decide whether follow-up or escalation is appropriate.';if(s==='needs_user_action')return'Complete the official agency step and return with the confirmation number.';if(s==='closed')return'This case is closed.';return'Review the routing plan and continue the complaint process.'}
async function addCaseEvidence(files){if(!activeCase)return;const valid=files.filter(f=>f.size<=8388608);for(const f of valid){try{const base64=await fileToBase64(f);await call({action:'upload_evidence',case_number:activeCase.case_number,guest_token:activeCase.guest_token,file_name:f.name,content_type:f.type||'application/octet-stream',byte_size:f.size,evidence_type:guessEvidenceType(f.name),description:'Attached from Case Command Center.',base64})}catch(e){toast('Evidence upload error: '+e.message);return}}toast(valid.length+' file'+(valid.length===1?'':'s')+' attached.');openCase(activeCase.case_number)}
function guessEvidenceType(n){const t=n.toLowerCase();if(/jpg|jpeg|png|heic|gif/.test(t))return'photo';if(/pdf/.test(t))return'document';if(/mp4|mov|avi|webm/.test(t))return'video';if(/mp3|wav|m4a|aac/.test(t))return'audio';if(/eml|msg/.test(t))return'email';return'other'}
async function addResponse(){if(!activeCase)return;const body=$('responseBody').value.trim();if(!body){toast('Paste the agency response first.');return}try{await call({action:'add_response',case_number:activeCase.case_number,guest_token:activeCase.guest_token,route_id:selectedRoute?.id||null,source_label:$('responseSource').value.trim()||'Agency response',body});$('responseBody').value='';$('responseSource').value='';saveIndex({...activeCase,status:'response_received'});toast('Response added to the case.');openCase(activeCase.case_number)}catch(e){toast(e.message)}}
async function closeCase(){if(!activeCase)return;try{await call({action:'close_case',case_number:activeCase.case_number,guest_token:activeCase.guest_token,resolution:$('resolution').value.trim()||'Case closed by user.'});saveIndex({...activeCase,status:'closed'});toast('Case closed.');openCase(activeCase.case_number)}catch(e){toast(e.message)}}
const reportInput=$('reportEvidenceInput'),reportDrop=$('reportDrop');if(reportInput)reportInput.addEventListener('change',e=>{acceptFiles([...e.target.files]);e.target.value=''});if(reportDrop){['dragenter','dragover'].forEach(ev=>reportDrop.addEventListener(ev,e=>{e.preventDefault();reportDrop.classList.add('drag')}));['dragleave','drop'].forEach(ev=>reportDrop.addEventListener(ev,e=>{e.preventDefault();reportDrop.classList.remove('drag')}));reportDrop.addEventListener('drop',e=>acceptFiles([...e.dataTransfer.files]))}
$$('[data-go]').forEach(b=>b.onclick=()=>show(b.dataset.go));$('hot').onclick=()=>{show('home');setTimeout(()=>$('report').scrollIntoView({behavior:'smooth'}),80)};$('example').onclick=()=>{$('story').value="My mom was at a nursing home in Van Nuys and fell in the hallway. Staff didn't tell our family until the next morning. She had pain afterward, and the facility has not answered my questions about whether an incident report was completed.";$('reportApprox').value='yesterday';$('story').focus()};$('start').onclick=start;$('next').onclick=next;$('back').onclick=back;$('strengthBack').onclick=()=>{activeQuestions=[...questions];step=0;renderQuestion();show('intake')};$('buildPlan').onclick=buildPlan;$('copy').onclick=copyMemo;$('edit').onclick=()=>{step=0;renderQuestion();show('intake')};$('primaryAction').onclick=handoff;$('saveConfirmation').onclick=saveConfirmation;$('backCases').onclick=()=>show('cases');$('caseEvidenceInput').addEventListener('change',e=>{addCaseEvidence([...e.target.files]);e.target.value=''});$('saveResponse').onclick=addResponse;$('closeCase').onclick=closeCase;
})();