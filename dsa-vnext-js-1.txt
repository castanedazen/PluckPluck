
(()=>{
const API='https://pbesndmimhujqivviqpw.supabase.co/functions/v1/case-agent';
const $=id=>document.getElementById(id), $$=s=>[...document.querySelectorAll(s)];
const esc=s=>(s??'').toString().replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const state={story:'',where:'',incidentDate:'',incidentApprox:'',name:'',who:[],impact:[],tried:[],evidence:[],outcomes:[],files:[],analysis:null};
let step=0, activeQuestions=[], routes=[], caseNum='', token='', selectedRoute=null, activeCase=null;
const railLabels=['Story','Key facts','Impact','Evidence','Outcome','Action plan','Filing','Tracking'];
function toast(msg){$('toast').textContent=msg;$('toast').style.display='block';setTimeout(()=>$('toast').style.display='none',3000)}
function show(id){$$('.stage').forEach(x=>x.classList.remove('on'));$(id).classList.add('on');window.scrollTo({top:0,behavior:'smooth'});if(id==='cases')renderCases()}
function renderRail(id,current){$(id).innerHTML=railLabels.map((x,i)=>`<div class="railStep ${i<current?'done':i===current?'on':''}"><i>${i<current?'✓':String(i+1).padStart(2,'0')}</i>${x}</div>`).join('')}
async function call(body){const r=await fetch(API,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.detail||j.error||'Request failed');return j}
function emergencyInfo(story){const t=story.toLowerCase();if(/right now|immediate danger|can'?t breathe|unconscious|overdose|weapon|fire|bleeding heavily/.test(t))return{level:'emergency',title:'THIS MAY NEED IMMEDIATE HELP FIRST.',text:'If someone is in immediate danger or there is a medical emergency, use emergency services before continuing a regulatory complaint. DSA can still help organize the accountability case afterward.'};if(/elder abuse|child abuse|unsafe discharge|eviction tomorrow|utility shutoff|retaliation|fraud in progress/.test(t))return{level:'urgent',title:'THIS MAY BE TIME-SENSITIVE.',text:'The facts you described may need faster action than an ordinary complaint. DSA will keep the regulatory path separate from emergency or urgent help.'};return null}
function localExtract(story){const t=story.toLowerCase(), dates=[...story.matchAll(/\b(?:jan(?:uary)?|feb(?:ruary)?|mar(?:ch)?|apr(?:il)?|may|jun(?:e)?|jul(?:y)?|aug(?:ust)?|sep(?:t(?:ember)?)?|oct(?:ober)?|nov(?:ember)?|dec(?:ember)?)\s+\d{1,2}(?:,\s*\d{4})?|\b\d{1,2}\/\d{1,2}\/\d{2,4}\b/ig)].map(m=>m[0]).slice(0,4);const money=[...story.matchAll(/\$[\d,]+(?:\.\d{2})?/g)].map(m=>m[0]).slice(0,4);let category='consumer';if(/hospital|doctor|physician|nurse|nursing|medical|health plan|insurance|clinic|patient|skilled nursing|snf/.test(t))category='healthcare';else if(/landlord|rent|apartment|tenant|mold|housing|property manager/.test(t))category='housing';else if(/employer|workplace|wage|paycheck|osha|manager at work/.test(t))category='workplace';else if(/bank|credit|loan|debt|mortgage|lender/.test(t))category='financial';const orgGuess=(story.match(/(?:at|by|from|with|against)\s+([A-Z][A-Za-z0-9&.' -]{2,55}?)(?=\s+(?:in|on|and|because|where|who|that|when)\b|[,.]|$)/)||story.match(/\b([A-Z][A-Za-z0-9&.' -]{2,50}(?:Hospital|Medical Center|Health Plan|Insurance|Nursing|Care Center|Clinic|Bank|Company|LLC|Inc\.?|Apartments|Property Management))\b/)||[])[1]||'';const loc=(story.match(/\b(?:in|near|at)\s+([A-Z][A-Za-z .'-]{2,32},\s*(?:CA|California))\b/)||[])[1]||'';const who=[];if(category==='healthcare'){if(/health plan|insurer|insurance/.test(t))who.push('Health plan / insurer');else if(/doctor|physician|therapist|psychiatrist|licensed professional/.test(t))who.push('Doctor or licensed professional');else who.push('Healthcare facility')}else if(category==='housing')who.push('Landlord / property');else if(category==='workplace')who.push('Employer');else if(category==='financial')who.push('Bank / lender');else if(/contractor|repair|service company/.test(t))who.push('Contractor / service company');const impact=[];if(/injur|pain|hospitalized|medical harm|health harm|fell|fall/.test(t))impact.push('Physical injury / health harm');if(/unsafe|danger|hazard|risk/.test(t))impact.push('Safety risk');if(/\$|refund|charged|money|financial loss|bill/.test(t))impact.push('Financial loss');if(/denied|refused|wouldn'?t provide|didn'?t provide|no service/.test(t))impact.push('Denied care or service');if(/discriminat|civil rights|race|disability|sex|gender|religion/.test(t))impact.push('Discrimination / rights issue');if(/fraud|scam|decept|lied about|false charge/.test(t))impact.push('Fraud / deception');if(/retaliat|punished me|threatened me after/.test(t))impact.push('Retaliation');if(/neglect|poor quality|ignored|failed to|didn'?t tell|did not tell/.test(t))impact.push('Poor quality / neglect');const tried=[];if(/call(?:ed|ing)?/.test(t))tried.push('Called them');if(/email(?:ed|ing)?|wrote|letter/.test(t))tried.push('Emailed / wrote them');if(/grievance|complaint to the plan|internal complaint/.test(t))tried.push('Filed an internal grievance');if(/supervisor|manager/.test(t))tried.push('Asked for a supervisor');if(/appeal(?:ed|ing)?/.test(t))tried.push('Appealed a decision');const evidence=[];if(/photo|video|picture|screenshot/.test(t))evidence.push('Photos / video');if(/email|text message|texts|message/.test(t))evidence.push('Emails / texts');if(/bill|receipt|invoice/.test(t))evidence.push('Bills / receipts');if(/medical record|chart|visit summary/.test(t))evidence.push('Medical records');if(/contract|policy|agreement/.test(t))evidence.push('Contract / policy');if(/letter|notice/.test(t))evidence.push('Letters / notices');if(/witness|saw it|heard it/.test(t))evidence.push('Witnesses');const outcomes=[];if(/investigat|look into|review/.test(t))outcomes.push('Investigation');if(/fix|correct|repair|change/.test(t))outcomes.push('Correct the problem');if(/refund|reimburse|pay me|payment/.test(t))outcomes.push('Refund / payment');if(/reverse|overturn.*denial|approve/.test(t))outcomes.push('Reverse a denial');if(/safety|make it safe/.test(t))outcomes.push('Safety fix');if(/disciplin|license/.test(t))outcomes.push('Discipline / licensing review');if(/stop retaliation/.test(t))outcomes.push('Stop retaliation');if(/explanation|answer me|written response/.test(t))outcomes.push('Written explanation');return{category,dates,money,orgGuess,locationGuess:loc,who:[...new Set(who)],impact:[...new Set(impact)],tried:[...new Set(tried)],evidence:[...new Set(evidence)],outcomes:[...new Set(outcomes)]}}
const questions=[
 {key:'where',title:'WHERE DID THIS HAPPEN?',why:'Location can completely change which city, state, or federal authority has jurisdiction.',type:'text',placeholder:'City, state — or as specific as you know'},
 {key:'who',title:'WHO OR WHAT WAS RESPONSIBLE?',why:'We use this to distinguish a facility, professional, company, landlord, insurer, employer, government office, and other regulated entities.',type:'choice',choices:['Healthcare facility','Doctor or licensed professional','Health plan / insurer','Landlord / property','Employer','Bank / lender','Contractor / service company','Government agency','Other']},
 {key:'name',title:'WHAT IS THEIR NAME?',why:'The exact organization or person helps us build a usable complaint and narrow licensing routes.',type:'text',placeholder:'Organization, facility, company, or professional name'},
 {key:'impact',title:'WHAT WAS THE IMPACT?',why:'Agencies often care about the type and seriousness of harm. Choose everything that actually applies.',type:'choice',multi:true,choices:['Physical injury / health harm','Safety risk','Financial loss','Denied care or service','Discrimination / rights issue','Fraud / deception','Retaliation','Poor quality / neglect','No direct harm yet']},
 {key:'tried',title:'WHAT HAVE YOU ALREADY TRIED?',why:'Some regulators require you to complain to the organization first. Others do not. This changes the filing path.',type:'choice',multi:true,choices:['Called them','Emailed / wrote them','Filed an internal grievance','Asked for a supervisor','Appealed a decision','Nothing yet']},
 {key:'evidence',title:'WHAT DO YOU HAVE?',why:'We do not need every file right now. We need to know what evidence exists so the complaint can reference it accurately.',type:'choice',multi:true,choices:['Photos / video','Emails / texts','Bills / receipts','Medical records','Contract / policy','Letters / notices','Witnesses','No evidence yet']},
 {key:'outcomes',title:'WHAT DO YOU WANT TO HAPPEN?',why:'A regulator decides what it can do, but your requested outcome helps us write a focused complaint instead of an angry essay.',type:'choice',multi:true,choices:['Investigation','Correct the problem','Refund / payment','Reverse a denial','Safety fix','Discipline / licensing review','Stop retaliation','Written explanation','Other']}
];
function buildQuestionQueue(){activeQuestions=questions.filter(q=>{if(q.key==='where')return !state.where;if(q.key==='who')return !state.who.length;if(q.key==='name')return !state.name;if(q.key==='impact')return !state.impact.length;if(q.key==='tried')return !state.tried.length;if(q.key==='evidence')return !state.evidence.length&&!state.files.length;if(q.key==='outcomes')return !state.outcomes.length;return true})}
function inferredItems(ex){const rows=[];if(state.incidentDate||state.incidentApprox)rows.push(['WHEN',state.incidentDate||state.incidentApprox,'REPORTED / PROVIDED']);else if(ex.dates.length)rows.push(['WHEN',ex.dates[0],'INFERRED']);if(state.where||ex.locationGuess)rows.push(['WHERE',state.where||ex.locationGuess,state.where?'PROVIDED':'INFERRED']);if(state.name||ex.orgGuess)rows.push(['WHO',state.name||ex.orgGuess,state.name?'PROVIDED':'INFERRED']);rows.push(['LIKELY CATEGORY',ex.category,'INFERRED']);if(state.impact.length)rows.push(['IMPACT',state.impact.join(', '),'INFERRED FROM STORY']);if(state.evidence.length||state.files.length)rows.push(['EVIDENCE',state.evidence.length?state.evidence.join(', '):`${state.files.length} attached file${state.files.length===1?'':'s'}`,state.files.length?'ATTACHED':'INFERRED']);return rows}
function getVal(q){return state[q.key]}
function setVal(q,v){state[q.key]=v}
function renderQuestion(){renderRail('railSteps',Math.min(step+1,4));const q=activeQuestions[step];if(!q){renderStrength();return}$('progress').style.width=`${Math.round(((step+1)/Math.max(1,activeQuestions.length))*100)}%`;$('qTitle').textContent=q.title;$('qWhy').textContent=q.why;$('qBody').innerHTML='';if(q.type==='text'){const i=document.createElement('input');i.id='qInput';i.placeholder=q.placeholder||'';i.value=getVal(q)||'';$('qBody').appendChild(i);setTimeout(()=>i.focus(),20)}else if(q.type==='date'){$('qBody').innerHTML=`<div class="dateShell"><div class="dateBox"><label>EXACT DATE</label><input type="date" id="qDate" value="${esc(state.incidentDate||'')}"><div class="dateHint">Use this if you know the actual date.</div></div><div class="dateBox"><label>OR APPROXIMATE TIMEFRAME</label><input type="text" id="qApprox" placeholder="Example: late August 2026, about two weeks ago" value="${esc(state.incidentApprox||'')}"><div class="dateHint">A month, week, range, or approximate date is enough.</div></div></div>`;setTimeout(()=>{const x=$('qDate');if(x&&!x.value)$('qApprox').focus()},20)}else{const g=document.createElement('div');g.className='choiceGrid';q.choices.forEach(c=>{const b=document.createElement('button');b.type='button';b.className='choice';const cur=getVal(q);if(q.multi?(cur||[]).includes(c):cur===c)b.classList.add('on');b.textContent=c;b.onclick=()=>{if(q.multi){let a=[...(getVal(q)||[])];a=a.includes(c)?a.filter(x=>x!==c):[...a,c];setVal(q,a)}else setVal(q,c);renderQuestion()};g.appendChild(b)});$('qBody').appendChild(g)}$('back').disabled=false}
function saveCurrent(){const q=activeQuestions[step];if(q.type==='text'){const v=$('qInput').value.trim();if(!v){toast('Add the missing detail or go back.');return false}setVal(q,v)}else if(q.type==='date'){const exact=$('qDate').value.trim(),approx=$('qApprox').value.trim();if(!exact&&!approx){toast('Add an exact date or an approximate timeframe.');return false}state.incidentDate=exact;state.incidentApprox=approx}else{const v=getVal(q);if(!v||(Array.isArray(v)&&!v.length)){toast('Choose at least one option.');return false}}return true}
function formatBytes(n){if(n<1024)return n+' B';if(n<1048576)return (n/1024).toFixed(1)+' KB';return (n/1048576).toFixed(1)+' MB'}
function renderReportFiles(){const el=$('reportFileList');if(!el)return;el.innerHTML=state.files.length?state.files.map((f,i)=>`<div class="attachedFile"><span><b>${esc(f.name)}</b><br><span class="micro">${formatBytes(f.size)}</span></span><button type="button" data-rm="${i}">REMOVE</button></div>`).join(''):'';$$('[data-rm]').forEach(b=>b.onclick=()=>{state.files.splice(Number(b.dataset.rm),1);renderReportFiles()})}
function acceptFiles(files){const ok=[];for(const f of files){if(f.size>8388608){toast(`${f.name} is over 8 MB.`);continue}ok.push(f)}state.files=[...state.files,...ok].slice(0,8);renderReportFiles()}
function fileToBase64(file){return new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split(',')[1]||'');r.onerror=reject;r.readAsDataURL(file)})}
async function uploadCaseFiles(){if(!state.files.length)return;const u=$('uploadState');for(let i=0;i<state.files.length;i++){const f=state.files[i];if(u)u.textContent=`SECURING EVIDENCE ${i+1}/${state.files.length}: ${f.name}`;const base64=await fileToBase64(f);await call({action:'upload_evidence',case_number:caseNum,guest_token:token,file_name:f.name,content_type:f.type||'application/octet-stream',byte_size:f.size,evidence_type:guessEvidenceType(f.name),description:'Attached by complainant during report intake.',base64})}if(u)u.textContent=`✓ ${state.files.length} FILE${state.files.length===1?'':'S'} SECURELY ATTACHED TO CASE ${caseNum}`;}
function start(){const s=$('story').value.trim();if(s.length<15){toast('Give us a little more to work with — one or two sentences is enough.');return}const exact=($('reportDate')?.value||'').trim(),approx=($('reportApprox')?.value||'').trim();state.incidentDate=exact;state.incidentApprox=approx;state.story=s;state.analysis=localExtract(s);const ex=state.analysis;if(!state.incidentDate&&!state.incidentApprox&&ex.dates.length)state.incidentApprox=ex.dates[0];if(!state.where&&ex.locationGuess)state.where=ex.locationGuess;if(!state.name&&ex.orgGuess)state.name=ex.orgGuess;if(!state.who.length&&ex.who.length)state.who=ex.who;if(!state.impact.length&&ex.impact.length)state.impact=ex.impact;if(!state.tried.length&&ex.tried.length)state.tried=ex.tried;if(!state.evidence.length&&ex.evidence.length)state.evidence=ex.evidence;if(!state.outcomes.length&&ex.outcomes.length)state.outcomes=ex.outcomes;buildQuestionQueue();const u=emergencyInfo(s);if(u){$('urgencyBox').className=`alert ${u.level}`;$('urgencyBox').innerHTML=`<h3>${u.title}</h3><p>${u.text}</p>`}else $('urgencyBox').className='alert hidden';const clues=inferredItems(ex);$('qBody').innerHTML='';$('qTitle').textContent="HERE'S WHAT WE UNDERSTOOD.";$('qWhy').textContent='We separate what you told us from what DSA inferred. We will only ask for missing details that can change the route, complaint, evidence, urgency, or remedy.';$('qBody').innerHTML=`<div class="inferenceBoard">${clues.map(([k,v,src])=>`<div class="inferenceItem"><b>${esc(k)}</b><span>${esc(v)}</span><em>${esc(src)}</em></div>`).join('')}</div><div class="intakePromise"><b>${activeQuestions.length?`${activeQuestions.length} material question${activeQuestions.length===1?'':'s'} left.`:'No material questions left.'}</b> We are not going to make you repeat facts already in the story.</div><div class="row"><button class="btn ghost" type="button" id="correctInferences">CHECK / CORRECT THESE DETAILS</button></div>`;$('progress').style.width=activeQuestions.length?'10%':'100%';renderRail('railSteps',1);step=-1;show('intake');const ci=$('correctInferences');if(ci)ci.onclick=()=>{activeQuestions=[...questions];step=0;renderQuestion()}}
function next(){if(step===-1){if(!activeQuestions.length){renderStrength();return}step=0;renderQuestion();return}if(!saveCurrent())return;if(step<activeQuestions.length-1){step++;renderQuestion()}else renderStrength()}
function back(){if(step<=-1){show('home');return}if(step===0){step=-1;start();return}step--;renderQuestion()}
function list(vals, fallback){return vals&&vals.length?vals:fallback}
function renderStrength(){renderRail('strengthRail',4);const have=[];const missing=[];const boost=[];const noise=[];have.push(`A first-hand narrative about ${state.name||'the reported organization'}`);if(state.where)have.push(`Location: ${state.where}`);if(state.incidentDate||state.incidentApprox)have.push(`When it happened: ${state.incidentDate||state.incidentApprox}`);if(state.impact.length)have.push(`Impact identified: ${state.impact.join(', ')}`);if(state.tried.length&&!state.tried.includes('Nothing yet'))have.push(`Prior resolution attempts: ${state.tried.join(', ')}`);if(!state.incidentDate&&!state.incidentApprox)missing.push('A date or approximate timeframe may help an investigator locate records.');if(!state.name)missing.push('The organization or professional name is still unclear.');if(state.evidence.includes('No evidence yet'))boost.push('Contemporaneous emails, photos, notices, or records could strengthen the complaint if available.');else boost.push(`Reference the evidence you already have: ${state.evidence.join(', ')}.`);if(/nursing|hospital|doctor|medical|health/i.test(state.story))boost.push('For healthcare complaints, identify the facility/provider, incident date, and any related medical or incident records.');noise.push('You do not need to prove the entire case before filing a complaint.');noise.push('Avoid speculation about motives unless you have evidence; focus on what happened and what you want reviewed.');$('strengthGrid').innerHTML=[['good','WHAT WE